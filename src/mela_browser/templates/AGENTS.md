# Template Conventions

* We use shoelace components for all styling.

# CSS overrides

* Only use css when absolutely necessary and only after prompting.
* Keep css overrides in pug templates, no css files.
* Use nested css to keep clear separation.